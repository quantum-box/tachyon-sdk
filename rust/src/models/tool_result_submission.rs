/*
 * Tachyon API
 *
 * Tachyon Platform REST API
 *
 * The version of the OpenAPI document: 0.58.1
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ToolResultSubmission : Result submitted by the client for a pending tool call.  After receiving a `tool_call_pending` SSE event, the client executes the tool and submits the result via `POST /v1/llms/chatrooms/{chatroom_id}/agent/tool-result`.  # Example  ```json {   \"tool_id\": \"ct_01JEXAMPLE\",   \"result\": \"{\\\"rows\\\": [{\\\"id\\\": 1, \\\"name\\\": \\\"Alice\\\"}]}\",   \"is_error\": false } ```
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ToolResultSubmission {
    /// Set to `true` if the tool execution failed. The error message in `result` is shown to the LLM so it can attempt recovery or report the failure.
    #[serde(rename = "is_error", skip_serializing_if = "Option::is_none")]
    pub is_error: Option<bool>,
    /// The tool execution result as a string. For structured data, serialize to JSON string. The content is passed directly to the LLM as the tool's output.
    #[serde(rename = "result")]
    pub result: String,
    /// The `tool_id` from the `tool_call_pending` SSE event that this result corresponds to.
    #[serde(rename = "tool_id")]
    pub tool_id: String,
}

impl ToolResultSubmission {
    /// Result submitted by the client for a pending tool call.  After receiving a `tool_call_pending` SSE event, the client executes the tool and submits the result via `POST /v1/llms/chatrooms/{chatroom_id}/agent/tool-result`.  # Example  ```json {   \"tool_id\": \"ct_01JEXAMPLE\",   \"result\": \"{\\\"rows\\\": [{\\\"id\\\": 1, \\\"name\\\": \\\"Alice\\\"}]}\",   \"is_error\": false } ```
    pub fn new(result: String, tool_id: String) -> ToolResultSubmission {
        ToolResultSubmission {
            is_error: None,
            result,
            tool_id,
        }
    }
}

