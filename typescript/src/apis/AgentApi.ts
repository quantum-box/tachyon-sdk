/* tslint:disable */
/* eslint-disable */
/**
 * Tachyon API
 * Tachyon Platform REST API
 *
 * The version of the OpenAPI document: 0.49.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AgentChunk,
  AgentExecuteRequest,
  AgentMessagesResponse,
  AgentStatusResponse,
} from '../models/index';
import {
    AgentChunkFromJSON,
    AgentChunkToJSON,
    AgentExecuteRequestFromJSON,
    AgentExecuteRequestToJSON,
    AgentMessagesResponseFromJSON,
    AgentMessagesResponseToJSON,
    AgentStatusResponseFromJSON,
    AgentStatusResponseToJSON,
} from '../models/index';

export interface ExecuteAgentRequest {
    chatroomId: string;
    xOperatorId: string;
    authorization: string;
    agentExecuteRequest: AgentExecuteRequest;
}

export interface GetAgentMessagesRequest {
    chatroomId: string;
    xOperatorId: string;
    authorization: string;
    limit?: number | null;
    offset?: number | null;
}

export interface GetAgentStatusRequest {
    chatroomId: string;
    xOperatorId: string;
    authorization: string;
}

/**
 * 
 */
export class AgentApi extends runtime.BaseAPI {

    /**
     * Creates request options for executeAgent without sending the request
     */
    async executeAgentRequestOpts(requestParameters: ExecuteAgentRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['chatroomId'] == null) {
            throw new runtime.RequiredError(
                'chatroomId',
                'Required parameter "chatroomId" was null or undefined when calling executeAgent().'
            );
        }

        if (requestParameters['xOperatorId'] == null) {
            throw new runtime.RequiredError(
                'xOperatorId',
                'Required parameter "xOperatorId" was null or undefined when calling executeAgent().'
            );
        }

        if (requestParameters['authorization'] == null) {
            throw new runtime.RequiredError(
                'authorization',
                'Required parameter "authorization" was null or undefined when calling executeAgent().'
            );
        }

        if (requestParameters['agentExecuteRequest'] == null) {
            throw new runtime.RequiredError(
                'agentExecuteRequest',
                'Required parameter "agentExecuteRequest" was null or undefined when calling executeAgent().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOperatorId'] != null) {
            headerParameters['x-operator-id'] = String(requestParameters['xOperatorId']);
        }

        if (requestParameters['authorization'] != null) {
            headerParameters['Authorization'] = String(requestParameters['authorization']);
        }


        let urlPath = `/v1/llms/chatrooms/{chatroom_id}/agent/execute`;
        urlPath = urlPath.replace(`{${"chatroom_id"}}`, encodeURIComponent(String(requestParameters['chatroomId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AgentExecuteRequestToJSON(requestParameters['agentExecuteRequest']),
        };
    }

    /**
     * Execute a task using the agent system. Returns an SSE stream of `AgentChunk` events.  ## Client-Side Tools  You can provide custom tool definitions via the `client_tools` field. When the LLM decides to call one of these tools, the server emits a `tool_call_pending` SSE event containing the tool name, arguments, and a unique `tool_id`. The client should:  1. Execute the tool locally using the provided arguments 2. Submit the result via    `POST /v1/llms/chatrooms/{chatroom_id}/agent/tool-result`    with the matching `tool_id` 3. The agent resumes processing with the tool result  For **fire-and-forget** tools (`fire_and_forget: true`), the server immediately returns a success message to the LLM without waiting for a result submission. The client still receives the `tool_call_pending` event and can execute the tool, but does not need to submit a result.  Tool names must not collide with server built-in tools (e.g. `read_file`, `execute_command`). The server validates this and returns an error if a collision is detected.  When `client_tools` is provided, JSON tool call mode is automatically enabled — you do not need to set `use_json_tool_calls` separately.
     * Execute agent
     */
    async executeAgentRaw(requestParameters: ExecuteAgentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AgentChunk>> {
        const requestOptions = await this.executeAgentRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AgentChunkFromJSON(jsonValue));
    }

    /**
     * Execute a task using the agent system. Returns an SSE stream of `AgentChunk` events.  ## Client-Side Tools  You can provide custom tool definitions via the `client_tools` field. When the LLM decides to call one of these tools, the server emits a `tool_call_pending` SSE event containing the tool name, arguments, and a unique `tool_id`. The client should:  1. Execute the tool locally using the provided arguments 2. Submit the result via    `POST /v1/llms/chatrooms/{chatroom_id}/agent/tool-result`    with the matching `tool_id` 3. The agent resumes processing with the tool result  For **fire-and-forget** tools (`fire_and_forget: true`), the server immediately returns a success message to the LLM without waiting for a result submission. The client still receives the `tool_call_pending` event and can execute the tool, but does not need to submit a result.  Tool names must not collide with server built-in tools (e.g. `read_file`, `execute_command`). The server validates this and returns an error if a collision is detected.  When `client_tools` is provided, JSON tool call mode is automatically enabled — you do not need to set `use_json_tool_calls` separately.
     * Execute agent
     */
    async executeAgent(requestParameters: ExecuteAgentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AgentChunk> {
        const response = await this.executeAgentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getAgentMessages without sending the request
     */
    async getAgentMessagesRequestOpts(requestParameters: GetAgentMessagesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['chatroomId'] == null) {
            throw new runtime.RequiredError(
                'chatroomId',
                'Required parameter "chatroomId" was null or undefined when calling getAgentMessages().'
            );
        }

        if (requestParameters['xOperatorId'] == null) {
            throw new runtime.RequiredError(
                'xOperatorId',
                'Required parameter "xOperatorId" was null or undefined when calling getAgentMessages().'
            );
        }

        if (requestParameters['authorization'] == null) {
            throw new runtime.RequiredError(
                'authorization',
                'Required parameter "authorization" was null or undefined when calling getAgentMessages().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOperatorId'] != null) {
            headerParameters['x-operator-id'] = String(requestParameters['xOperatorId']);
        }

        if (requestParameters['authorization'] != null) {
            headerParameters['Authorization'] = String(requestParameters['authorization']);
        }


        let urlPath = `/v1/llms/chatrooms/{chatroom_id}/agent/messages`;
        urlPath = urlPath.replace(`{${"chatroom_id"}}`, encodeURIComponent(String(requestParameters['chatroomId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Get agent message log for specified chatroom.
     * Get agent message log
     */
    async getAgentMessagesRaw(requestParameters: GetAgentMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AgentMessagesResponse>> {
        const requestOptions = await this.getAgentMessagesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AgentMessagesResponseFromJSON(jsonValue));
    }

    /**
     * Get agent message log for specified chatroom.
     * Get agent message log
     */
    async getAgentMessages(requestParameters: GetAgentMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AgentMessagesResponse> {
        const response = await this.getAgentMessagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getAgentStatus without sending the request
     */
    async getAgentStatusRequestOpts(requestParameters: GetAgentStatusRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['chatroomId'] == null) {
            throw new runtime.RequiredError(
                'chatroomId',
                'Required parameter "chatroomId" was null or undefined when calling getAgentStatus().'
            );
        }

        if (requestParameters['xOperatorId'] == null) {
            throw new runtime.RequiredError(
                'xOperatorId',
                'Required parameter "xOperatorId" was null or undefined when calling getAgentStatus().'
            );
        }

        if (requestParameters['authorization'] == null) {
            throw new runtime.RequiredError(
                'authorization',
                'Required parameter "authorization" was null or undefined when calling getAgentStatus().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOperatorId'] != null) {
            headerParameters['x-operator-id'] = String(requestParameters['xOperatorId']);
        }

        if (requestParameters['authorization'] != null) {
            headerParameters['Authorization'] = String(requestParameters['authorization']);
        }


        let urlPath = `/v1/llms/chatrooms/{chatroom_id}/agent/status`;
        urlPath = urlPath.replace(`{${"chatroom_id"}}`, encodeURIComponent(String(requestParameters['chatroomId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Get execution status of agent for specified chatroom.
     * Get agent status
     */
    async getAgentStatusRaw(requestParameters: GetAgentStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AgentStatusResponse>> {
        const requestOptions = await this.getAgentStatusRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AgentStatusResponseFromJSON(jsonValue));
    }

    /**
     * Get execution status of agent for specified chatroom.
     * Get agent status
     */
    async getAgentStatus(requestParameters: GetAgentStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AgentStatusResponse> {
        const response = await this.getAgentStatusRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
